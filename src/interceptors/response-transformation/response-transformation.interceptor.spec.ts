import { ResponseTransformationInterceptor } from './response-transformation.interceptor';

describe('ResponseTransformationInterceptor', () => {
  it('should be defined', () => {
    expect(new ResponseTransformationInterceptor()).toBeDefined();
  });
});
